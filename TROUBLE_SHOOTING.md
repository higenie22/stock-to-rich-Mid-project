# 프로젝트 핵심 Troubleshooting

## 1. 연구 목표를 검증 가능한 문장으로 정의

프로젝트를 처음 설계할 때의 목표는 “안정적인 종목 중에서 손해 없이 돈을 벌 수 있는 종목을 추천합니다”라는 방향에 가까웠습니다. 그러나 이 표현은 실제 연구 가설로 사용하기 어렵습니다. 주식에 100% 투자하는 이상 손실 가능성을 제거할 수 없고, 과거 데이터에서 최대낙폭이 작았다는 사실도 미래 손실 한도를 보장하지 않기 때문입니다. 또한 안정적인 종목을 찾는 문제와 높은 수익을 낼 종목을 찾는 문제는 서로 다른 과제이므로, 하나의 모호한 목표로 묶으면 어떤 결과를 성공으로 볼 것인지도 불분명해집니다.

이 문제를 해결하기 위해 연구 목표를 “과거 정보만을 이용해 S&P 500 종목을 상대적 위험 수준에 따라 분류하고, 그중 Stable 군집에서 향후 시장 대비 상대수익이 높을 것으로 예상되는 Top-10 종목을 선정합니다. 주식 100% 투자 조건에서 과거 OOS 포트폴리오의 MDD를 15% 이내로 억제하는 것을 목표로 검증합니다”로 바꾸었습니다. 이 정의에서는 안정성 분류와 수익성 예측의 역할이 분리됩니다. K-Means는 변동성과 시장 민감도를 이용해 투자 후보군을 좁히고, 수익성 모델은 그 후보군 안에서 종목의 상대 순위를 결정합니다. MDD 15% 역시 보장 조건이 아니라 성과를 판단하기 위한 검증 목표로 한정했습니다.

이렇게 목표를 수정하면서 결과를 평가하는 기준도 명확해졌습니다. 안정성 단계에서는 미래 변동성, 미래 MDD와 False-Stable 비율을 확인하고, 수익성 단계에서는 날짜별 Rank IC, Top-Bottom spread와 Top-10 포트폴리오 성과를 확인합니다. 마지막에는 수익률만 보는 것이 아니라 MDD, 변동성, 섹터 집중도와 정산 가능 여부를 함께 평가합니다. 따라서 일부 기간의 수익률이 높다는 이유만으로 전체 연구 목표가 달성됐다고 판단하지 않습니다.

## 2. Purge 적용 이후에도 선택편향이 남는 이유

시계열 데이터를 Train과 Test로 나누고 Purge까지 적용하면 모델이 Test의 미래 정보를 학습하는 문제는 막을 수 있습니다. 현재 파이프라인도 각 검증 시작일보다 과거에 있는 행 중 `target_end < fold_start`와 `label_available_at < fold_start`를 모두 만족하는 데이터만 학습에 사용합니다. 예를 들어 120거래일 수익률의 계산 기간이 검증 Fold와 겹치는 행이나, 당시에는 아직 결과를 알 수 없었던 행은 Train에서 제외됩니다. 이 처리는 전형적인 look-ahead leakage를 막기 위한 조치입니다.

그러나 Purge는 학습 데이터 안으로 미래 정보가 들어가는 것을 막을 뿐, 연구자가 OOS 결과를 본 뒤 의사결정을 바꾸는 문제까지 해결하지는 못합니다. 모델이 Test 데이터를 직접 학습하지 않았더라도, 사람이 Test 수익률과 MDD를 확인한 뒤 피처, 모델, 예측 방향, 보유기간 또는 앙상블 비중을 변경하면 Test 결과가 간접적으로 모델 선택에 사용됩니다. 이 시점부터 해당 Test는 완전히 독립된 최종 OOS가 아니라 여러 번 관측된 개발 홀드아웃에 가까워집니다.

이를 줄이기 위해 최종 추천 모델의 비중은 OOS 수익률이 아니라 2020~2022년 개발 CV의 날짜별 IC로 정하도록 수정했습니다. 각 모델의 평균 CV IC가 양수일 때만 `max(mean_cv_ic, 0)`을 이용해 ReLU 비중을 부여하고, 음수인 모델은 최종 비중에서 제외합니다. 모든 모델의 CV IC가 유효하지 않거나 0 이하이면 OOS 성과가 좋은 모델을 대신 선택하거나 임의의 균등 비중을 사용하지 않고 추천을 보류합니다. 이렇게 고정된 비중만 이후 OOS Fold와 최신 추천에 적용합니다.

다만 이 수정으로 연구 전체의 선택편향이 완전히 사라지는 것은 아닙니다. 프로젝트를 진행하는 동안 최근 OOS 구간의 여러 실험 결과를 이미 확인했기 때문에, 연구 구조 자체에는 반복 관찰에 따른 잔존 편향이 있을 수 있습니다. 따라서 현재 결과는 “OOS 성과를 직접 이용해 최종 앙상블 비중을 선택하는 문제를 제거했습니다”라고 설명하는 것이 정확합니다. 완전히 새로운 전진 검증은 모델, 피처, 방향과 비중을 실제로 고정한 뒤 생성한 추천을 변경하지 않고 미래 120거래일 동안 평가해야 합니다.

## 3. Target의 정의 문제

초기 모델에서는 미래 로그수익률을 Target으로 사용했지만, 프로젝트의 실제 목적은 단순히 가격이 많이 오른 종목을 찾는 것이 아니라 같은 기간 시장보다 더 나은 종목을 Stable 후보군 안에서 고르는 것이었습니다. 절대수익률을 사용하면 강한 상승장에서는 대부분 종목의 Target이 양수가 되고, 모델이 시장 전체의 상승을 예측하는 것만으로도 성능이 좋아 보일 수 있습니다. 반대로 시장 전체가 하락한 기간에는 시장보다 덜 하락한 종목도 절대수익률이 음수라는 이유로 나쁜 종목처럼 처리됩니다.

이 문제를 해결하기 위해 기본 Target을 같은 보유기간의 단순 상대수익률로 변경했습니다. 현재 정의는 `종목 미래 단순수익률 - 시장 미래 단순수익률`입니다. 종목과 시장은 같은 신호일, 진입일과 청산일을 사용하고, 신호 다음 거래일 종가에 진입해 정해진 거래일 후의 종가로 정산합니다. 단순수익률과 로그수익률을 한 식에서 혼합하지 않도록 했으며, 기업행동이나 가격 누락으로 신뢰할 수 없는 Target은 결측 상태로 유지합니다.

Target을 상대수익률로 변경한 것은 학습 문제의 정의를 바꾼 것이지 포트폴리오의 실제 손익을 상대수익률로 대체한 것은 아닙니다. 모델의 선별력은 상대수익률과 Rank IC로 평가하지만, 실제 Top-10 포트폴리오의 누적수익률과 MDD는 종목의 실제 진입·청산 가격 경로를 이용해 계산합니다. 또한 20일, 60일과 120일 Target은 서로 다른 예측 문제로 취급하며, 120일 결과가 좋다는 이유만으로 다른 기간의 Target이나 정산값을 재사용하지 않습니다.

## 4. 모델링 과정에서 발생한 문제

### 4.1 상승 예측 비율이 1.0으로 나온 문제

실제 데이터에서는 평균 수익률이 음수인 종목도 존재했지만 회귀 모델의 상승 예측 비율이 1.0으로 나타났습니다. 이는 모든 종목이 반드시 오를 것이라는 강한 신호라기보다, 회귀 모델의 예측값이 양의 평균 부근으로 과도하게 수축된 결과였습니다. 미래 수익률 분포에는 큰 음수와 양수 값이 함께 존재하지만, MSE를 최소화하는 모델과 강한 규제는 극단값을 정확하게 맞히기보다 전체 평균에 가까운 값을 출력하는 경향이 있습니다. 이때 실제 Target의 표준편차보다 예측값의 표준편차가 매우 작아지고, 모든 예측값이 0보다 조금 큰 범위에 모일 수 있습니다.

이 문제를 확인하기 위해 Train과 Validation의 Target 분포, 실제 양수 비율, 예측 양수 비율, 실제값과 예측값의 표준편차를 별도로 비교했습니다. 또한 Ticker를 모델 피처에서 제외하더라도 Date와 Ticker 인덱스를 평가용 메타데이터로 보존해 종목별 실제값과 예측값이 올바르게 연결되는지 확인했습니다. `ndarray` 예측값을 원본 DataFrame에 위치만으로 붙이지 않고, Validation 인덱스를 가진 `Series`로 변환해 정렬 오류를 방지했습니다.

최종적으로 상승·하락 비율은 핵심 성능지표에서 제외하고 횡단면 순위 예측력에 초점을 맞췄습니다. 같은 날짜의 후보 종목 안에서 예측값을 백분위 순위로 바꾼 뒤 날짜별 Rank IC와 Top-Bottom spread를 평가하며, 최종 앙상블도 모델별 원시 예측값의 크기가 아니라 후보군 내 백분위 순위를 결합합니다. 따라서 모든 원시 예측값이 양수이더라도 종목 간 순위를 충분히 구분하는지는 별도로 판단할 수 있습니다. 반대로 Rank IC까지 0에 가깝다면 양수 예측 비율과 관계없이 모델의 실질적인 선별 정보가 부족한 것으로 해석합니다.

### 4.2 음수 IC에서 신호 부호를 반전한 문제

일부 CV에서는 예측 순위와 실제 상대수익률 순위가 반대로 움직이면서 IC가 음수로 나타났습니다. 처음에는 IC의 절대값이 크다면 예측 신호를 반대로 사용해도 의미 있는 수익을 얻을 수 있다는 아이디어가 있었습니다. 수학적으로 동일 표본에서 `pred_reverse = -pred`를 적용하면 Rank IC의 부호가 바뀌므로 가능성 자체는 있습니다. 그러나 Test나 OOS 결과를 확인한 뒤 부호를 뒤집으면 방향 선택도 하나의 사후 모델 선택이 되어 선택편향이 발생합니다.

이를 해결하기 위해 방향 반전이 필요한 실험에서는 과거 개발 CV만으로 방향을 결정하도록 했습니다. H3-3의 방향 진단은 2020~2022년의 과거 스냅샷에서 120일 기본 피처의 Fold별 IC를 계산하고, 그 평균 부호를 기준으로 하나의 방향을 고정합니다. 이후 외부 평가에서는 Fold별 결과에 따라 방향을 다시 바꾸지 않고 동일한 부호를 사용합니다. `abs(IC)`만 비교해 매 Fold에서 더 유리한 방향을 선택하거나, OOS 결과를 보고 정방향과 역방향 중 하나를 고르는 방식은 사용하지 않습니다.

최종 추천 앙상블에서는 음수 IC 모델을 무조건 뒤집는 대신 ReLU 비중으로 제외하는 더 보수적인 방식을 사용합니다. 즉 개발 CV에서 양의 평균 IC가 확인된 모델만 가중치를 받고, 음수 모델이 실제로 안정적인 역방향 신호인지 별도 검증되지 않았다면 최종 추천에 포함하지 않습니다. 이를 통해 일시적인 음수 IC나 시장 국면 변화로 발생한 역상관을 확정적인 역방향 알파로 오해하는 문제를 줄였습니다.

### 4.3 시계열 데이터에 일반적인 Fold를 적용한 문제

일반적인 `KFold`나 무작위 분할은 미래 관측치가 과거 관측치의 학습에 들어갈 수 있고, 120거래일처럼 긴 미래 라벨 기간이 Train과 Validation 사이에서 겹칠 수 있습니다. 또한 이 데이터는 같은 날짜에 여러 종목이 존재하는 패널 구조이므로 행 단위로 무작위 분할하면 동일한 시장 국면의 종목들이 Train과 Validation에 동시에 섞입니다. 이 경우 표본 수는 많아 보이지만 실제로는 같은 날짜와 중첩된 가격 경로를 반복 사용해 성능을 과대평가할 수 있습니다.

이 문제는 날짜 기준의 Purged Walk-forward 방식으로 해결했습니다. Fold를 행 번호가 아니라 검증 시작일로 정의하고, 각 Fold에서는 검증일보다 과거인 데이터만 학습에 사용합니다. 그중에서도 미래 Target 종료일과 실제 라벨 이용 가능일이 모두 Fold 시작일보다 앞선 행만 남깁니다. Validation과 OOS 평가는 같은 날짜의 횡단면 종목을 하나의 그룹으로 유지하고, 날짜별 IC를 먼저 계산한 뒤 Fold 수준으로 집계합니다.

유효 표본 수도 전체 행 수만으로 설명하지 않도록 수정했습니다. 수십만 개의 종목-날짜 행이 있더라도 독립적인 시계열 사건 수는 리밸런싱 시점과 정산된 보유기간 수에 더 가깝기 때문입니다. 결과표에는 전체 행 수와 함께 Fold 수, 리밸런싱 시점 수, 정산된 기간 수, Fold별 IC와 `max_label_available`을 기록해 실제 검증 범위를 확인할 수 있도록 했습니다.

## 5. 생존편향 제거 문제

현재 S&P 500 구성종목만 사용해 과거 10년을 분석하면, 그 사이 상장폐지·인수합병·실적 악화로 지수에서 제외된 종목이 처음부터 존재하지 않았던 것처럼 처리됩니다. 반대로 최근에 편입된 성공적인 종목은 편입 이전 기간에도 투자할 수 있었던 것처럼 포함될 수 있습니다. 이런 생존편향은 과거 수익률을 높이고 위험을 낮아 보이게 만들 가능성이 있습니다. 가격 데이터에 특정 종목의 행이 존재한다는 사실도 그 날짜에 그 종목이 S&P 500 구성종목이었다는 의미는 아닙니다. 지수에서 빠진 종목이 그 뒤로도 수년간 정상적으로 거래되는 경우가 흔하기 때문입니다.

이를 줄이기 위해 현재 종목 목록이 아니라 과거 편입·편출 이력(구간 1,259개, 고유 티커 1,206개)을 수집하고, 분석 기간 중 한 번이라도 S&P 500에 포함됐던 종목으로 수집 대상을 확장했습니다. 각 신호일에는 `start_date <= Date`이고 `end_date`가 없거나 `Date < end_date`인 구간이 하나라도 있는 종목만 후보로 남깁니다. 한 종목이 여러 번 편입·편출될 수 있으므로 티커당 하나의 구간만 가정하지 않습니다(52개 티커가 구간 2개 이상, AAL은 1996~1997과 2015~2024). 그 결과 리밸런싱 간격과 무관하게 (종목, 날짜) 행의 22%가 제외되고, 120일 스냅샷의 시점당 후보는 중앙값 497개로 실제 지수 규모와 맞습니다. 생존편향이 실제로 줄었는지는 표본 구성으로 확인할 수 있습니다 — 스냅샷에 등장하는 675종목 중 183종목(27%)이 이미 편출된 종목이며, 이 값이 0에 가깝다면 필터가 작동하지 않은 것입니다.

Point-in-time 필터만으로는 부족했습니다. 멤버십 이력은 당시 티커로 기록되고 가격은 현재 티커로 수집되므로, 그 사이 티커가 바뀐 종목은 둘이 만나지 못하고 조용히 사라집니다. BNY(뉴욕멜론은행)는 10년 내내 구성종목이고 가격도 2,637행을 모두 갖고 있었지만 이력이 옛 티커 `BK`로 남아 스냅샷에 한 번도 등장하지 않았습니다. 매핑을 추측으로 만들지 않기 위해 SEC EDGAR의 CIK와 `formerNames`로 법인 동일성을 확인하는 절차를 거쳐 23건을 확정했고, 종목 수가 707개에서 710개로 늘었습니다. 옛 티커의 `end_date`와 새 티커의 `start_date`가 맞물리는지 보는 날짜 경계 매칭은 무관한 두 종목이 같은 날 교체돼도 같은 패턴이 나오므로 검증 보조로만 쓰고 신규 탐지에는 쓰지 않습니다.

법인이 같다는 것과 매핑이 실제로 작동한다는 것은 다른 문제여서 리밸런싱 스냅샷에서 한 번 더 검증했습니다. 각 종목의 스냅샷 등장 횟수를 통합된 멤버십 구간의 기대 횟수와 대조한 결과, 가격이 있는 22건 전부가 정확히 일치했습니다(BNY 0회 → 22회, 부분 기간인 BFH·FRCB는 각각 10회·9회). 같은 대조를 710종목 전체로 확대하니 기대와 실제가 다른 종목이 29개 나왔지만, 그 차이가 난 (종목, 날짜) 중 가격이 실제로 존재하는 경우는 0건이었습니다. 즉 남은 불일치는 매칭 실패가 아니라 그 날짜에 가격이 없어서 생긴 것이며, BNY와 같은 누락은 남아 있지 않습니다.

반대로 과잉 매핑은 서로 다른 회사의 데이터를 이어 붙이므로 더 위험합니다. HFC→DINO, CCE→CCEP, WRK→SW는 CIK가 다른 신설 법인이라 개명이 아닌 합병으로 보고 제외했습니다. WRK→SW를 매핑하면 SW가 상장 8년 전 스냅샷부터 후보로 잡혀 등장 횟수가 4회에서 18회로 늘어나는데, 그렇게 추가되는 구간은 거래량 중앙값이 0인 비정상 시계열입니다. 현금 인수(CSRA→GD, JNPR→HPE, NFX→OVV)는 인수 주체의 주가 이력이 피인수 종목 자리에 들어가므로 매핑 금지 대상으로 명시했습니다. 다만 FRC→FRCB처럼 제외하는 쪽이 오히려 편향을 만드는 경우는 포함했습니다. 동일 법인이고 파산 전 실거래 이력이 유효하므로, 빼면 "구성종목이 4개월 만에 −97%"라는 실제 사건이 표본에서 사라져 위험이 과소평가됩니다. 파산 후 좀비 구간은 멤버십 필터가 편출일에서 이미 차단합니다.

수익률 정산 단계에서는 미래 가격이 없는 종목을 평가 전에 제거하거나, 해당 종목을 제외한 뒤 남은 종목의 비중을 다시 100%로 만드는 방식을 사용하지 않습니다. 확인되지 않은 수익은 결측으로 유지하고 해당 기간을 `incomplete` 또는 미정산으로 표시합니다. 상장폐지나 최종 정산 누락이 성과에 미치는 영향을 보기 위해 -50% 손실을 가정한 결과도 별도 민감도 분석으로 제공하지만, 이를 실제 수익률이나 생존편향 보정값으로 간주하지 않습니다.

이 조치로 현재 구성종목만 과거에 소급하는 가장 큰 문제는 줄였지만 생존편향이 완전히 제거된 것은 아닙니다. 무료 소스로 끝내 복원하지 못한 종목이 파산·청산(BBBY, ENDP, MNK), 현금 인수(CSRA, JNPR, NFX), 합병 후 상장폐지(COG, CBS, ESV)의 세 유형으로 남아 있고, 상장폐지 시점의 최종 수익률과 M&A 현금 정산액, 당시 시점의 섹터·재무정보도 완전히 복원되지 않습니다. 다만 이 누락은 방법론이 아니라 데이터 소스의 한계이며, 상장폐지까지 이력을 보존하는 CRSP 같은 소스로 바꾸면 상당수는 복구됩니다. 따라서 결과에서는 "point-in-time membership을 적용해 생존편향을 완화했습니다"라고 표현하며 "생존편향을 완전히 제거했습니다"라고 주장하지 않습니다.

## 6. 하락장과 횡보장 국면에 대한 대비

최근 OOS 구간만으로 모델을 평가하면 특정 시장 환경에 맞는 전략을 전체 시장에서 강건한 전략으로 오해할 수 있습니다. 특히 2024~2026년은 대형 기술주와 유동성의 영향이 컸던 제한된 국면이므로, 이 구간에서 나온 네 번 안팎의 120일 평가만으로 금융위기, 급격한 금리 인상기 또는 장기 횡보장에서도 같은 성과가 재현된다고 말할 수 없습니다. 상승장에서 대부분의 Target이 양수가 되는 현상도 모델의 선별력을 실제보다 좋아 보이게 할 수 있습니다.

이를 완화하기 위해 모델과 앙상블 비중을 최근 OOS에서 선택하지 않고, 코로나 급락이 있었던 2020년, 유동성 장세의 2021년, 금리 인상과 하락장이 있었던 2022년을 포함한 개발 CV에서 결정했습니다. 날짜 순서를 유지한 Walk-forward Fold를 사용해 각 시점에서 이용 가능한 과거 데이터만 학습했고, Fold별 IC의 평균뿐 아니라 부호가 국면마다 일관되는지도 확인했습니다. 종목의 절대수익률 대신 시장 대비 상대수익률을 Target으로 사용해 시장 전체의 상승과 하락을 종목 선별력으로 잘못 평가하는 문제도 줄였습니다.

모델 구성에서도 기본 기술·위험 피처만 사용하는 모델과 시장·거시 피처를 추가한 Macro-LightGBM을 별도로 비교하고, 최종 앙상블에서는 개발 CV에서 실제로 양의 IC를 보인 모델만 사용합니다. Stable 군집은 변동성과 `|Beta|`를 이용해 시장 충격에 상대적으로 덜 민감한 후보군을 먼저 구성하며, 단일 섹터 집중이 방어주 베팅으로 변질되지 않도록 섹터 상한과 섹터 익스포저를 함께 확인합니다.

다만 이러한 조치는 하락장과 횡보장에 대한 대비를 강화한 것이지 모든 국면에서의 성과를 입증한 것은 아닙니다. 개발 CV에 포함된 위기 구간도 제한적이고, 최근 OOS는 하나의 시장 체제를 주로 반영합니다. 따라서 발표에서는 “서로 다른 개발 국면을 포함해 규칙을 고정했습니다”라고 설명하되, “하락장에서도 수익이 보장됩니다”라고 표현하지 않습니다. 향후에는 더 긴 point-in-time 데이터, 국면별 독립 성과, 시작일 Offset Sweep과 거래비용을 포함한 전진 검증이 추가로 필요합니다.
